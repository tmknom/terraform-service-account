[
  {
    "name": "${container_name}",
    "image": "${nginx_image}",
    "essential": true,
    "portMappings": [
      {
        "protocol": "tcp",
        "containerPort": ${container_port}
      }
    ]
  }
]
